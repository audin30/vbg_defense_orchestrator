{
  "mcpServers": {
    "orchestrator-postgres": {
      "command": "uvx",
      "args": ["postgres-mcp", "--access-mode=restricted"],
      "env": {
        "DATABASE_URI": "postgresql://orchestrator:orchestrator@localhost:5432/orchestrator"
      }
    }
  }
}
